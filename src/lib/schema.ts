import {
  users,
  members,
  workspaces,
  projects,
  tasks,
  storeItems,
  redemptionRequests,
  memberBadges,
  passwordResetTokens,
} from "./schemas_drizzle";

export const schema = {
  users,
  members,
  workspaces,
  projects,
  tasks,
  storeItems,
  redemptionRequests,
  memberBadges,
  passwordResetTokens,
};
